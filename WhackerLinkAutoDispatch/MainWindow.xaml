<Window x:Class="WhackerLinkAutoDispatch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WhackerLink Auto Dispatch" Height="600" Width="1000" Background="#1E1E2F">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Load Template Button -->
        <Button Content="Load Template" Height="40" Margin="10" HorizontalAlignment="Left" Click="LoadTemplate_Click"/>

        <!-- Channel Selection Dropdown -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Select Channel:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Name="ChannelSelector" Width="300" Height="30"/>
        </StackPanel>

        <!-- Dynamic Fields Section -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Name="DynamicFieldsPanel" Margin="20"/>
        </ScrollViewer>

        <!-- Dispatch Now Button -->
        <Button Content="Dispatch Now" Height="50" Width="200" HorizontalAlignment="Right" Margin="10"
                VerticalAlignment="Bottom" Click="DispatchNow_Click"/>
    </Grid>
</Window>
